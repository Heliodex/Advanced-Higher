-- templating engine for luau and html iguess
-- like etlua and penlight.template
-- not similar to lux or jsx or (sadly) htmlx

local template = {}
template.__index = template

function template.parse(temp: string)
	
end

return template
